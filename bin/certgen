#!/usr/bin/env ruby
# frozen_string_literal: true

# ---
# Certgen Bootstrapper
# This entry point configures the load path and delegates execution to the CLI handler.
# ---

# Append the local 'lib' directory to the $LOAD_PATH.
# This ensures that 'require certgen' resolves to our local implementation
# rather than an installed gem version, facilitating standalone portability.
$LOAD_PATH.unshift File.expand_path('../lib', __dir__)

require 'certgen'

# Invoke the CLI entry point with the provided command-line arguments.
# We rescue common high-level exceptions here if needed, though most
# error handling is encapsulated within the CLI class itself.
Certgen::CLI.start(ARGV)
